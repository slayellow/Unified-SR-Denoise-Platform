# Unified SR·Denoise Platform 개발 여정

> 2026년 2월부터 8월까지, 엣지 NPU 위에서 초해상(SR)과 디노이즈 모델을 학습·양자화·배포하며 남긴 기록.
> 회사 GitLab 197커밋 + 개인 GitHub 미러 62커밋, 그리고 커밋에 함께 실린 실험일지·진단 문서를 근거로 재구성했다.
> 본문의 커밋 해시는 내부 저장소 기준이다.

---

## 한눈에 보기

| 시기 | 장 | 한 줄 요약 |
|---|---|---|
| 2018–2021 | 프롤로그 | Kaggle → 세그멘테이션 → 캘리브레이션 → TensorRT, 그리고 AIMET 연습장 |
| 2026.02–03 | 1장 | SR+Denoise 통합 플랫폼 탄생, AIMET 7단계 파이프라인 정립 |
| 2026.04 | 2장 | 노이즈 시뮬레이터와의 전쟁 — Color Inversion, Green Tint, V5(순수 AWGN) |
| 2026.05 | 3장 | 수치와 육안의 괴리 — "Denoise 먼저" 피벗, 4,555줄 폐기 |
| 2026.06–07 | 4장 | 42장 프로브와 KD의 종말, 6주의 침묵, Bicubic++ 등장 |
| 2026.08 초중순 | 5장 | HTP와의 싸움 — PixelShuffle 98%, 그리고 대청소(96건 리메디에이션) |
| 2026.08.19–21 | 6장 | 대진단 — "원인은 셋, 모델은 무죄" → release v1 동결 |
| 2026.08.22–25 | 7장 | 판결 — 색 얼룩의 진범은 W8A8 → 전 모델 W16A16, release v2 동결 |
| 2026.08.25– | 에필로그 | v3 — 실기 영상이 다시 뒤집은 것들 |

여정 전체를 관통하는 주제는 하나다. **합성 지표와 실제 화면 사이의 간극.** 검증셋에서 이긴 모델이 실기에서 지고, 무죄로 판결한 용의자가 다른 죄목으로 유죄가 되고, 계획서가 실측에 뒤집히는 일이 반복됐다. 이 문서는 그 반복을 시간순으로 따라간다.

---

## 프롤로그 — 이 여정 전에

2018년 겨울 Kaggle(Digit Recognizer, RSNA 폐렴 검출, 고래 식별)로 시작해, 2021년에는 비포장도로 주행가능영역 세그멘테이션과 LiDAR-카메라 외부 캘리브레이션(CalibNet/CalibDNN PyTorch 재구현), 2024년 TensorRT 프레임워크 자작까지 — "모델을 만드는 일"보다 **"모델을 장비에 싣는 일"** 쪽으로 무게가 옮겨온 이력이 이 프로젝트의 배경이다.

직접적인 준비운동은 2026년 2월의 `ultralytics_aimet` 저장소였다. Ultralytics YOLO 모델에 Qualcomm AIMET을 적용하는 연습장으로, "FP32 기준 측정 → 양자화 민감도 그리드 → 레이어별 QuantAnalyzer → Manual Mixed Precision → QAT → export"의 5단계 워크플로를 CLI로 정형화했다. 여기서 익힌 방법론과 `htp_v73` 설정 경험이 그대로 본 플랫폼의 양자화 파이프라인이 된다.

4월에는 `GStreamer-Topology`라는 사이드 도구도 만들었다 — GStreamer 파이프라인 텍스트를 파싱해 토폴로지 그래프로 시각화하는 Tauri 데스크톱 앱. 변환된 모델이 삽입되는 운용 파이프라인을 눈으로 점검하기 위한 자작 도구로, 모델 릴리스와 장비 배포 사이의 가교였다.

---

## 1장 — 플랫폼의 탄생 (2026.02–03)

**2026-02-06, 저장소 초기화** (`a06f351`). 흩어져 있던 SR 자산(QuickSRNet, SVSRNet 등)과 디노이즈 모델을 한 코드베이스로 통합했다. 처음부터 세 가지가 함께 설계됐다:

- **학습 엔진** — Hugging Face Accelerate 기반, Single/Multi-GPU 전환
- **AIMET 양자화 파이프라인** — `1_measure_fp32` → `2_compress_svd_pruning` → `3_analyze_quant_sensitivity` → `4_quant_analyzer` → `5_manual_mixed_precision` → `6_apply_qat` → `7_inference_qat`의 7단계 스크립트
- **Streamlit WebUI** — 학습·실험 관리 대시보드

2월 20일에는 QuickSRNet-Large IR을 pruning 60% + W4A8이라는 공격적인 설정으로 QAT까지 돌려본 기록이 남아 있다. 결과적으로 채택되지 않은 방향이지만, "얼마나 줄일 수 있는가"의 하한을 일찍 탐색해 본 흔적이다.

**3월 9일** (`476982d`) 자체 아키텍처(SVFocusSRNet, SVFocusDenoiseNet)로 전환하고 첫 배포 라인을 확정했다. 이때의 디노이즈 모델(260309, charbonnier+edge+ssim+hotpixel)이 이후 반 년간 모든 비교의 기준선이 된다: **PSNR 31.45 dB / SSIM 0.8938 / LPIPS 0.1575.**

초기의 잔실수들도 기록할 가치가 있다. ONNX와 체크포인트를 git에 커밋했다가 지우기를 세 번 반복한 끝에 "가중치는 ignore, 설정과 로그만 git으로" 원칙이 4월에야 자리잡았다.

---

## 2장 — 노이즈 시뮬레이터와의 전쟁 (2026.04)

4월은 디노이즈 학습용 노이즈 시뮬레이터를 세 번 갈아엎은 달이다. 매번 "모델이 이상하게 동작한다"에서 출발해 데이터 생성 파이프라인의 물리적 결함으로 귀결됐다.

**전투 1 — Color Inversion** (04-15, `8674ac2`). 파란 표지판이 붉게 출력됐다. 원인은 `cv2.COLOR_BayerRG2RGB` — OpenCV의 BGR 파이프라인과 RGGB 모자이크 가정이 충돌해 훈련 데이터의 B/R 채널이 스왑된 채 188 에폭을 학습했다. "딥러닝 모델이 파란색을 빨간색으로 매핑하도록 잘못 학습됨." 순수 파란색 픽셀이 온전히 보존되는지 검증 스크립트로 확인한 뒤 전면 재학습.

**전투 2 — Green Tint와 Edge Crushing** (04-16, `6342660`). 색이 정상화되자 숨어 있던 문제가 드러났다 — 화면 전체의 초록 틴트와 글씨 뭉개짐. 원인은 Bayer 데모자이킹 시뮬레이션이 만드는 Chromatic Fringing이었다:

> "모델은 이 찌꺼기의 보색인 초록색 필터를 전체 화면에 덮어씌워 보완하려 했고(Green Tint), … 차라리 글씨를 뿌옇게 뭉개버리는(Edge Crushing) 타협을 해버림."

물리적으로 정교해 보이던 Bayer 시뮬레이션 자체를 폐기하고 RGB 도메인 노이즈로 교체했다. 앞선 채널 스왑 버그가 오히려 이 문제를 가려 왔다는 점이 교훈적이다.

**전투 3 — Ghosting** (04-17, `dfc605e`). 새 엔진(V4)도 하루 만에 문제를 냈다. 나무줄기가 반투명해지고 바닥 질감이 플라스틱처럼 뭉개졌다. 원인 분석이 인상적이다:

> "3×3 가우시안 노이즈 덩어리 패턴이 드론 원거리 영상에 찍힌 얇은 나뭇가지 굵기(약 2~3픽셀)와 완전히 일치."

노이즈의 공간 스케일이 실제 구조물의 스케일과 겹치면 모델이 구조를 노이즈로 오인한다. 블러를 삭제하고 1픽셀 단위 순수 AWGN으로 바꾼 V5가 **PSNR 30.06 dB(V4 대비 +3.5 dB)** 를 기록하며 이 전쟁을 끝냈다.

**실측의 시작.** 같은 달, 대상 센서(MC-G105)를 주간/야간 × 줌 1x~7x로 직접 캡처해 배포 모델을 정량 평가했다. 결과는 뼈아팠다 — 야간 노이즈 억제 "사실상 0%", 야간 7x에서 고주파 에너지 −65%, 가짜 엣지(oversharpening) +41%. 이 표가 이후 모든 손실 가중치 결정의 근거가 됐고, 월말에 센서 특화 파인튜닝(dim32_v2, 0.0295M 파라미터 / 27.07 GFLOPs)을 배포 후보로 올렸다.

이 달의 또 다른 산물이 **출퇴근 동기화 워크플로**다(사이드스토리 참조).

---

## 3장 — 수치와 육안의 괴리 (2026.05)

5월의 주제는 한 문장이다: **검증 지표가 좋아진 모델이 실기 화면에서 진다.**

**5월 7일, 피벗** (`93f1149`). SR 경량화 모델(Block4)이 검증 PSNR 하락폭을 잘 방어했음에도(−0.1~−0.5 dB) 실기기(QCS8550) 육안 평가에서 기대 이하로 판정됐다. 결론은 방향 전환이었다:

> "SR 모듈은 입력 노이즈를 증폭시키는 특성이 있으므로 … Denoise 모델을 고도화하여 깨끗한 원본을 확보하는 것이 우선."

**센서 프로파일링 3세대** (5/11–5/18). Auto exposure/WB 환경이라 물리 파라미터 추정은 포기하고("temporal noise는 센서 계수가 아니라 real-domain instability proxy로 사용해야 한다"), 2,700장 실측 기반의 경험적 열화 프로파일을 v1 → v2 → v3로 반복 설계했다. v3는 "deploy 대체 후보"까지 갔다 — noise proxy 1.399 vs 1.516, 체크포인트 70% 축소.

**5월 19일, 전량 폐기** (`3248473`). 출장에서의 고배율 비행 실증이 판정을 뒤집었다. 프로파일 설계 문서와 설정 24개 파일, **4,555줄을 하루에 삭제**하고 검증된 deploy 열화 베이스라인으로 복귀했다. 합성 지표 개선이 비행 화질을 담보하지 못한 두 번째 사례.

이후 teacher 학습 체제로 재정렬했다 — NAFNet(116M 파라미터)과 Restormer(26M)를 스크래치 학습. 이 과정에서 인프라 결함도 두 건 잡았다: DDP에서 rank별로 따로 계산한 PSNR 때문에 한쪽 rank만 체크포인트 저장에 진입해 collective 순서가 어긋나는 버그(`188c1c6`), 그리고 stdout을 캡처하지 않아 사인 불명이 된 Restormer 학습 사망 사건 — "durable log에 stdout/stderr를 남겨라"가 명문화된 계기다.

---

## 4장 — 42장 프로브와 KD의 종말 (2026.06–07)

**프로브 게이트의 확립** (6/2). 2,700장 캡처셋에서 time/weather/scene/zoom 조합마다 1장씩, **고정 42장 real-sensor probe**를 뽑았다. 이후 모든 모델은 검증셋 점수와 무관하게 이 42장을 통과해야 한다 — 이 프로젝트 평가 철학의 원형이다.

**KD의 하루** (6/5). Deploy-teacher STKD, NAFNet-teacher STKD, 듀얼 teacher MTKD(ToneGuard)까지 아침에 착수해 저녁에 전부 폐기했다. 검증에서는 STKD가 이겼다(PSNR 24.05 vs 22.77). 42장 프로브에서는 참패했다(lowfreq luma MAE 4.66 vs 0.25, risk score 4.3배). 결론이 절제돼 있다:

> "KD 접근 자체가 틀렸다고 보기보다는, Student 체급(44K 파라미터)과 KD target 범위를 재설계해야 한다."

**역설적 결말** (6/16, `cc93dd8`). 한 달 넘게 키운 teacher들을 42장에서 실측하니 — NAFNet 선명도 손실 −0.0%, Restormer −1.4%, **Deploy 디노이즈 −32.6%**. 강한 teacher를 만들려던 실험이 "현재 배포 모델이 사실상 블러 필터"라는 것과 "실기 raw가 이미 충분히 깨끗하다"는 것을 동시에 증명해 버렸다. 디노이즈 트랙은 "Deploy 유지"로 동결.

그리고 **6주의 침묵** (6/17–7/27). 저장소에 커밋이 없다. 능동 작업이 소진된 상태였다.

**7월 28일, Bicubic++ 등장** (`7e2a9a7`). 침묵을 깬 것은 새 경량 SR 아키텍처였다. 이틀 뒤 공식 구현에 맞춰 KneeLR 스케줄러를 재현 구현하고 3-스케일 비교를 실측했다. 첫 수치부터 이 모델의 성격이 드러난다 — x3에서 PSNR은 bicubic에 밀리지만(25.44 vs 25.98) NIQE는 압도(5.76 vs 7.16). **"PSNR을 내주고 지각 품질을 얻는" 트레이드오프**가 이후 두 달의 주인공이 된다.

---

## 5장 — HTP와의 싸움, 그리고 대청소 (2026.08.01–18)

**PixelShuffle 98% 사건** (8/13, `30b1da5`). Bicubic++ x4가 QCS8550에서 7.70 ms — conv 연산량이 5.4배 싼 모델이 기존 모델(7.72 ms)과 같은 속도였다. 프로파일링 결과 PixelShuffle 하나가 17.5M 사이클, **네트워크 전체의 98%**. 여기서 이 저장소 특유의 접근이 나온다 — 문제를 고치기 전에 비용 법칙부터 세운다:

> "A DepthToSpace on HTP costs ~296k cycles per blocksize², measured within 6.1% across two models and blocksizes 2/4/8."

stride-2 스템이 blocksize를 2×scale로 강제하던 구조를 재설계(`upsample_mode: auto`)해 x4를 3.24 ms로 — **4.24배** 개선. 품질은 오히려 올랐다. 같은 커밋에서 AIMET 내장 `htp_v73` 설정이 Conv+LeakyReLU를 supergroup으로 묶어 **8개 op 중 6개를 조용히 float으로 떨어뜨리면서 변환 성공을 보고하는** 함정도 발견해 커스텀 설정으로 봉인했다.

**대청소** (8/14–18). 101개 에이전트 병렬 코드 리뷰 + 적대적 검증으로 확정한 96건의 findings를 계획서(REMEDIATION_PLAN)로 만들고, 8/18 저녁 4시간 동안 배치 A~F로 소진했다. 파낸 것들의 무게가 상당하다:

- **모든 기존 체크포인트가 데이터의 96.4%로 학습됐다** — `'*.png'` 리터럴 글롭이 `.JPG` 377장을 조용히 버림. "failure mode가 크래시가 아니라 그럴듯한 숫자였다."
- **같은 체크포인트가 재는 스크립트에 따라 1.97 dB 갈라진다** — trainer 25.85 / evaluate 26.50 / 논문 관례 27.83 dB. "모델 변경 없이 생긴 1.97 dB 폭은 우리가 쫓는 대부분의 개선폭보다 넓다." PSNR/SSIM 정의를 단일 모듈로 통일하고, 체크포인트에 `metric_spec`을 새겼다 — "자(ruler)의 교체이지 모델의 교체가 아니다."
- **baseline보다 나쁜 파인튜닝이 배포되는 경로** — best 추적 초기화 결함으로, 배포 중인 파인튜닝 산출물 전부가 이 경로를 지났다.

인상적인 것은 계획서와 실측의 관계다. 계획서 항목이 실측과 충돌하면 실측이 이겼고, 그 기각 사유가 커밋에 남았다 — "Not done, because measurement contradicted the plan."

---

## 6장 — 대진단, 그리고 release v1 (2026.08.19–21)

**검증 자(ruler)의 교체** (8/19, `c81134a`). x3 파인튜닝이 기존 게이트에서 탈락했는데, 실제 학습 열화로 재생성한 검증셋에서는 승리했다. 유래를 알 수 없는 레거시 검증 LR은 bicubic 대비 ~20 DN이나 어긋나 있었다 — "다른 과목으로 시험을 치고 있었다." 게이트를 교체하고, 심사에서 진 최강 시도도 보존하도록 `best_attempt.pth`를 도입했다: "게이트 판정이 자기가 심사한 최강 시도를 조용히 버려서는 안 된다."

**IR의 색 발명 차단** (8/20, `c06a5cf`). 완전 무채색인 IR 검증 이미지에서 RGB perceptual 손실이 수 에폭 만에 채도를 50% 올리는 것을 실측 — "무채색 입력에서 손실 그래디언트가 채널별로 다른가"라는 단일 시험으로 손실함수 3종을 각각 판정했다(perceptual: gray 모드 신설, SSIM: 통과, Edge: BT.601 탈락 → 채널 평균).

**EO 대진단** (8/20, `073d86e`). "EO가 bicubic과 구분이 안 된다"는 현장 보고에 대해 42개 가설을 세우고 반증을 시도해 3개 원인으로 압축했다:

1. **학습 LR 생성** — `cv2.INTER_CUBIC` 축소는 안티에일리어싱이 없어, 모델은 실제 입력에 존재하지 않는 에일리어싱을 지우도록 학습됐다. 실카메라 롤오프 0.048 vs 학습 LR 0.379. 실기 입력에서 모델은 사실상 패스스루.
2. **W8A8 양자화** — 화면에서 모델과 bicubic을 가르는 것의 상당 부분이 복원 디테일이 아니라 양자화 잡음. 그리고 기존 게이트는 FP32만 채점해 왔다 — "지금까지 보고된 모든 dB는 FP32 수치다."
3. **비교 실험 설계** — EO 비교만 디노이즈된 프레임을 bicubic 앵커에도 먹여, 이미 연화된 그림 위에서 판정하고 있었다.

결정타는 용량 논쟁의 종결이었다: **배포 분포에서 2,916 파라미터짜리 선형 필터가 학습된 CNN을 1.25 dB 이긴다** — 문제는 모델 크기가 아니다.

**벤치마크의 재정의** (8/20, `7169f43`). 단독 실행 2.32 ms인 모델이 5개 동시 실행에서 7.31 ms — "단독 수치를 읽고 곱하면 비용을 약 3배 과소평가한다." 원인은 열이 아니라 경합임까지 분리 실측.

**release v1 동결** (8/21, `7e46b5b`). 보드에 올라가 있던 6개 모델을 매니페스트로 릴리스 정의 — "재빌드에 필요한 전부, 3.2 MB." 동시에 v1의 실체도 숫자로 남겼다: **6개 구성 중 5개에서 plain bicubic보다 엣지가 약했다** (최악 IR x3: GT 대비 58.8% vs bicubic 79.1%). 현장이 "flat하다"고 말해 온 것의 정체다. 같은 날 W8A8 정밀 실측(`W8A8_VERDICT.md`)은 "양자화는 뿌연 현상의 원인이 아니다(비용 0.13~0.53 dB) — 원인은 학습이다"로 1차 판결을 내렸다.

---

## 7장 — 판결, 그리고 release v2 (2026.08.22–25)

**IR 데이터셋 애블레이션** (8/22–24). 학습 소스 4종의 "가르칠 것"을 bicubic 왕복 PSNR로 정량화했다. LLVIP는 41 dB로 왕복을 버틴다 — 복원할 고주파가 없다는 뜻이다:

> "그 쌍에서 최적 출력은 bicubic 업샘플 그 자체라, 학습 신호는 '아무것도 하지 마라'다. … 44%를 흐림으로 학습하고 채점은 그중 하나도 안 하고 있었다."

RAYSENSE 1,844장 단독 채택, 검증셋은 홀드아웃 100장으로 재구축(구 검증셋은 학습셋과 17% 근접 중복이라 폐기).

**patch_size의 스케일 맹목** (8/23, `2ec644c`). x2는 6.6분/17GB인데 x3/x4는 39분/33–38GB — 아키텍처 차이처럼 보이던 것이, patch가 LR 기준이라 HR 크롭 픽셀 수가 1×/2.25×/4×로 달랐던 것. 게다가 언더사이즈 가드가 x4에서 학습 이미지의 60%를 조용히 건너뛰고 있었다.

**재기소, 그리고 유죄** (8/25, `4a79a83`). 8/21에 "뿌연 현상" 죄목으로 무죄 방면된 W8A8이, 나흘 뒤 **색 얼룩과 루마 그레인**의 진범으로 유죄 판결을 받았다. FP32 색은 애초에 bicubic보다 정확했다(GT 대비 1.270 vs 1.285) — W8A8을 거칠 때만 어긋난다. IR 루마도 마찬가지: FP32 출력 노이즈 1.291 → W8A8 1.928, "양자화가 49%를 더하며, 그것이 IR 스트림에 보이는 그레인이다."

같은 날의 두 가지 마무리:

- **무채색 출력 접기** (`15ac0b0`) — IR 출력 3채널을 하나의 공유 필터로 묶으면 "동일한 가중치는 동일하게 양자화"되어 채널간 색차가 **정확히 0.0000 DN**(v1은 113 DN)이 된다. 평균 연산을 그래프에 남기면 float 폴백으로 2.746 → 10.401 ms이므로, QAT가 자동으로 최종층 가중치에 접도록 내장 — "잊을 별도 단계가 없다."
- **release v2 동결** (`a03e8e3`) — "v1→v2는 한 줄이다: W8A8 → W16A16." 장비 실측 평탄부 크로마 σ가 FP32와 사실상 일치(EO x2: FP32 1.900 / W8A8 2.520 / W16A16 1.914). 선명도는 별도의 학습 수정(다운샘플 커널 다양화, 기하 증강 실제 적용, HR 크롭 통일)으로 6개 구성 전부 bicubic 상회. 대가는 속도 1.71–2.35× — "Qualcomm 문서에 INT16 지연 수치가 없어 직접 측정한 값이다."

---

## 에필로그 — v3, 실기 영상이 다시 뒤집은 것들 (2026.08.25–)

v2 동결 당일 오후, 실기 화면 녹화 영상(EO/IR 3-브랜치 비교)을 프레임 단위로 정량 분석하는 v3 조사가 시작됐다. 첫날의 발견 두 가지가 다시 한번 전제를 뒤집었다:

- **EO**: 합성 검증에서 bicubic을 이기던 v2가(88.6% vs 84.1%) 실기 화면에서는 전 프레임 열세(선명도비 0.813). 원인의 절반은 확정 — 실영상 유래 입력에서 모델 출력이 bicubic과 RMS 1.8 DN밖에 다르지 않은 **사실상 항등함수**였다. 6장의 진단("실기 입력에서 패스스루")이 v2 레시피 개선 이후에도 지속되고 있다는 뜻이다.
- **IR**: v2 매니페스트에 "컬럼 노이즈는 학습 GT에 내재"라고 적었는데, 당일 저녁 실측에서 재현에 실패했다 — **GT 3개 폴더 어디에도 컬럼 고정패턴이 없다.** 노이즈는 실기 센서 입력에 있고(전 열 미세 오프셋), 고전적 ISP 컬럼 보정 프로토타입이 SR 앞단에서 스트라이프를 87% 제거함을 확인했다. 매니페스트의 진단 절반은 하루를 못 넘겼다.

v3는 이렇게 시작된 상태다: 열화 매칭 재학습(디노이즈 인더루프 LR 생성), GV/FFT 손실, 학습-시 재파라미터화, ISP 컬럼 보정 통합이 대기 중이다.

---

## 사이드스토리 — 출퇴근 동기화 워크플로

개인 GitHub 저장소(본 문서가 실린 곳)의 62커밋은 별도의 프로젝트가 아니라 **회사 내부망 GitLab과 재택 사이의 이중 저장소 동기화 흔적**이다.

- **탄생** (4월 중순): 주말 재택 작업을 월요일에 합치는 과정에서 내부망/외부망 동시 접속 불가로 유실 위기를 겪고("Emergency Sync Restore: Recovered weekend home work from GitHub") 워크플로가 관례화됐다.
- **운영 방식**: 출근 시 GitHub→GitLab로 집 작업 수신·소스 덮어쓰기(단 실험일지·체크포인트·학습 로그는 절대 덮지 않음), 퇴근 시 실험일지를 "당일 스냅샷"으로 갱신하고 GitLab→GitHub로 소스만 rsync. 대용량 산출물은 회사에만, 소스+일지는 양방향.
- **성과**: 주말·휴일 재택 커밋이 미러에만 남아 있다. 가장 큰 것은 5/17(일) 하루에 작성된 **온라인 MTKD teacher 파이프라인 12파일 +2,151줄** — KD 트레이너, KD 손실, NAFNet/Restormer 모델 포팅, 설계문서까지.
- **종료** (6/16): 디노이즈 트랙 동결과 함께 재택에서 이어갈 작업이 소진됐고, 7월 말 재개된 작업은 DLC 변환·실기 벤치마크 등 장비 종속 단계여서 집에서 이어갈 수 없었다. 마지막 미러 커밋은 GitLab에 아직 커밋되지 않았던 워킹트리 파일까지 백업하고 있다 — 6주 뒤에야 GitLab에 커밋된 프로브 스크립트가 그 증거다.

---

## 이 여정에서 배운 것

1. **검증 지표는 실기 화면을 담보하지 않는다.** 5월의 SR 육안 탈락, 6월의 KD(검증 승/프로브 참패), 8월의 EO 진단, v3 첫날의 실기 역전까지 — 네 번 반복됐다. 대응책은 고정 실측 프로브(42장), 실기 유래 검증 트랙, 그리고 "게이트가 무엇을 채점하고 있는가"에 대한 상시 의심이었다.
2. **자(ruler)부터 통일하라.** 같은 모델이 재는 방법에 따라 1.97 dB 갈라진 순간, 그보다 작은 모든 개선 주장이 무의미해졌다. 지표 정의의 단일화와 체크포인트 내 `metric_spec` 기록이 그 답이었다.
3. **측정이 계획을 이긴다.** 리메디에이션 계획서의 항목이 실측과 충돌하면 실측이 이겼고, 그 기각도 기록으로 남겼다. "Not done, because measurement contradicted the plan."
4. **실패는 기록될 때만 자산이 된다.** Bayer 시뮬레이터 폐기, 4,555줄 프로파일 라인 폐기, KD 당일 폐기, 반나절 살고 사라진 도구들 — 각각의 사인(死因)이 커밋 본문에 남아 다음 결정의 근거가 됐다.
5. **양자화 이슈는 죄목을 나눠 다뤄야 한다.** W8A8은 "뿌연 현상"에는 무죄(0.13~0.53 dB), "색 얼룩·그레인"에는 유죄였다. 하나의 실험으로 하나의 죄목만 판결할 것.
6. **하드웨어에는 비용 법칙이 있다.** "DepthToSpace ≈ 296k cycles × blocksize²" 같은 실측 법칙 하나가 아키텍처 결정(업샘플 재설계, 4.24배)을 바꿨다. 단독 벤치마크는 동시 운용 비용을 3배 과소평가한다는 것도 실측으로만 알 수 있었다.
7. **노이즈 증강의 함정: 디노이징은 곧 블러다.** 노이즈를 많이 보여줄수록 모델은 지우는 쪽으로 기울고, 그 대가는 선명도다. 증강 강도의 상한을 정하는 것은 언제나 이 트레이드오프였다.
8. **동일한 가중치는 동일하게 양자화된다.** IR 채널간 색차 113 DN → 0.0000 DN은 학습 트릭이 아니라 구조적 불변식에서 나왔다. 양자화 오차를 "없애는" 가장 확실한 방법은 오차가 같아지게 만드는 것.

---

## 부록 — 저장소 안내

| 저장소 | 기간 | 커밋 | 내용 |
|---|---|---|---|
| 내부 GitLab (원본) | 2026-02-06 ~ | 197+ | 전체 개발 이력, 실험일지·진단 문서 포함 |
| GitHub `Unified-SR-Denoise-Platform` (본 저장소) | 2026-04-14 ~ 06-16 | 62 | 출퇴근 동기화 미러 + 주말 재택 커밋 |
| GitHub `ultralytics_aimet` | 2026-02 | — | AIMET 방법론 연습장 (프롤로그) |
| GitHub `GStreamer-Topology` | 2026-04~05 | — | 배포 파이프라인 시각화 도구 |

**시대별 대표 커밋** (내부 저장소 해시): 플랫폼 초기화 `a06f351` · 자체 모델 전환 `476982d` · Color Inversion 수정 `8674ac2` · V5 확정 `dfc605e` · 디노이즈 피벗 `93f1149` · 프로파일 라인 폐기 `3248473` · KD 폐기 `4d6c13c` · 디노이즈 동결 `cc93dd8` · Bicubic++ 등장 `7e2a9a7` · 업샘플 재설계 `30b1da5` · 자 통일 `6e23ed9` · 검증 게이트 교체 `c81134a` · EO 진단 `073d86e` · release v1 `7e46b5b` · 데이터셋 판결 `b7ec69e` · W8A8 유죄 `4a79a83` · 무채색 접기 `15ac0b0` · release v2 `a03e8e3`

**주요 문서 사료**: `docs/EO_DIAGNOSIS.md`(42개 가설 반증 진단), `docs/W8A8_VERDICT.md`(양자화 1차 판결), `results/release_v1·v2/MANIFEST.md`(릴리스 정의), REMEDIATION_PLAN(96건 리메디에이션, 8/21 은퇴), 그리고 4~6월의 `experiment.log`/`checklist.log`(출퇴근 핸드오프 일지, 커밋 히스토리로만 열람 가능).

---

*이 문서는 2026-08-25에 커밋 히스토리·실험일지·진단 문서를 근거로 작성됐다. 수치는 모두 당시 기록에서 그대로 인용했다.*
